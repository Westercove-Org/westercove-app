# Copy to .env and fill in. Read only by the `+api.ts` server routes, so it is
# never bundled into the app. Do not add an EXPO_PUBLIC_ prefix — that would
# compile the key into the client JS, where anyone can read it.
ANTHROPIC_API_KEY=sk-ant-...
