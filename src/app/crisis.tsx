import { CrisisInterface } from '@/features/safety/CrisisInterface';

/** Level 4 full-screen crisis interface. Reached at `/crisis`. */
export default function CrisisRoute() {
  return <CrisisInterface />;
}
