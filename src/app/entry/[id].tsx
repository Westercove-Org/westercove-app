import { EntryDetail } from '@/features/journal/EntryDetail';

/** Read an entry and its conversation thread. Reached at `/entry/:id`. */
export default function EntryDetailRoute() {
  return <EntryDetail />;
}
